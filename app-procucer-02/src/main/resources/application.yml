server:
  port: 8084
spring:
  application:
    name: app-producer-02
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
logging:
  level:
    #feign 日志以什么样的级别监控该接口
    org.example.feignclient: debug